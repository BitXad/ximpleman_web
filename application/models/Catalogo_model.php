<?php
/* 
 * Generated by CRUDigniter v3.2 
 * www.crudigniter.com
 */
 
class Catalogo_model extends CI_Model
{
    function __construct()
    {
        parent::__construct();
    }
    
    /*
     * Get catalogo by catalogo_id
     */
    function get_catalogo($catalogo_id)
    {
        $catalogo = $this->db->query("
            SELECT
                *

            FROM
                `catalogo`

            WHERE
                `catalogo_id` = ?
        ",array($catalogo_id))->row_array();

        return $catalogo;
    }
    
    /*
     * Get all catalogo count
     */
    function get_all_catalogo_count()
    {
        $catalogo = $this->db->query("
            SELECT
                count(*) as count

            FROM
                `catalogo`
        ")->row_array();

        return $catalogo['count'];
    }
        
    /*
     * Get all catalogo
     */
    function get_all_catalogo($params = array())
    {
        $limit_condition = "";
        if(isset($params) && !empty($params))
            $limit_condition = " LIMIT " . $params['offset'] . "," . $params['limit'];
        
        $catalogo = $this->db->query("
            SELECT
                *

            FROM
                `catalogo`

            WHERE
                1 = 1

            ORDER BY `catalogo_id` DESC

            " . $limit_condition . "
        ")->result_array();

        return $catalogo;
    }
        
    /*
     * function to add new catalogo
     */
    function add_catalogo($params)
    {
        $this->db->insert('catalogo',$params);
        return $this->db->insert_id();
    }
    
    /*
     * function to update catalogo
     */
    function update_catalogo($catalogo_id,$params)
    {
        $this->db->where('catalogo_id',$catalogo_id);
        return $this->db->update('catalogo',$params);
    }
    /*
     * function to truncar catalogo
     */
    function truncar_catalogo()
    {
        return $this->db->truncate('catalogo');
    }
    /*
     * function to delete catalogo
     */
    function delete_catalogo($catalogo_id)
    {
        return $this->db->delete('catalogo',array('catalogo_id'=>$catalogo_id));
    }
    /*
     * Get all catalogo ASC
     */
    function get_all_catalogo_asc($params = array())
    {
        $limit_condition = "";
        if(isset($params) && !empty($params))
            $limit_condition = " LIMIT " . $params['offset'] . "," . $params['limit'];
        
        $catalogo = $this->db->query("
            SELECT
                *

            FROM
                `catalogo`

            WHERE
                1 = 1

            ORDER BY `catalogo_id` ASC

            " . $limit_condition . "
        ")->result_array();

        return $catalogo;
    }
    /*
     * Get all catalogoes en orden alfabetico
     */
    function get_all_catalogoes()
    {
        $catalogo = $this->db->query("
            SELECT
                *
            FROM
                `catalogo`
            WHERE
                1 = 1
            ORDER BY `catalogo_nombre`
        ")->result_array();

        return $catalogo;
    }
    
}
