<?php 
/*
 Generated by Manuigniter v2.0 
 www.manuigniter.com
*/
class Parada extends CI_Controller{

    private $sistema;
    private $parametros;

 function __construct()
 {
       parent::__construct();
      $this->load->model('Parada_model');
      
        $this->load->model('Sistema_model');
        $this->load->model('Parametro_model');
        $this->sistema = $this->Sistema_model->get_sistema();
        $parametro = $this->Parametro_model->get_parametros();
        $this->parametros = $parametro[0];
        
        
        if ($this->session->userdata('logged_in')) {
            $this->session_data = $this->session->userdata('logged_in');
        }else {
            redirect('', 'refresh');
        }
 } 
 /*
* Listing of parada
 */
public function index()
{
     $data['sistema'] = $this->sistema;
  try{
      $data['noof_page'] = 0;
     $data['parada'] = $this->Parada_model->get_all_with_asso_parada();
      $data['_view'] = 'parada/index';
      $this->load->view('layouts/main',$data);
    } catch (Exception $ex) {
      throw new Exception('Parada Controller : Error in index function - ' . $ex);
  }  
}
 /*
  * Adding a new parada
  */
 function add()
 {  
     $data['sistema'] = $this->sistema;
try{
      $params = array(
       'estado_id'=> $this->input->post('estado_id'),
       'parada_nombre'=> $this->input->post('parada_nombre'),
       'parada_ubicacion'=> $this->input->post('parada_ubicacion'),
       'parada_latitud'=> $this->input->post('parada_latitud'),
       'parada_longitud'=> $this->input->post('parada_longitud'),
        );
       $this->load->library('upload');
       $this->load->library('form_validation');
       if(isset($_POST) && count($_POST) > 0)     
        {  
            $parada_id= $this->Parada_model->add_parada($params);
             $this->session->set_flashdata('alert_msg','<div class="alert alert-success text-center">Succesfully added.</div>');
              redirect('parada/index');
        }
        else
        { 
         $this->load->model('Estado_model');
         $data['all_estado'] = $this->Estado_model->get_all_estado(); 
           $data['_view'] = 'parada/add';
            $this->load->view('layouts/main',$data);
        }
  } catch (Exception $ex) {
    throw new Exception('Parada Controller : Error in add function - ' . $ex);
  }  
 }  
  /*
  * Editing a parada
 */
 public function edit($parada_id)
 {   
     $data['sistema'] = $this->sistema;
  try{
   $data['parada'] = $this->Parada_model->get_parada($parada_id);
       $this->load->library('upload');
       $this->load->library('form_validation');
     if(isset($data['parada']['parada_id']))
      {
        $params = array(
           'estado_id'=> $this->input->post('estado_id'),
           'parada_nombre'=> $this->input->post('parada_nombre'),
           'parada_ubicacion'=> $this->input->post('parada_ubicacion'),
           'parada_latitud'=> $this->input->post('parada_latitud'),
           'parada_longitud'=> $this->input->post('parada_longitud'),
        );
          if(isset($_POST) && count($_POST) > 0)     
           {  
           $this->Parada_model->update_parada($parada_id,$params);
             $this->session->set_flashdata('alert_msg','<div class="alert alert-success text-center">Succesfully updated.</div>');
                redirect('parada/index');
           }
           else
          {
             $this->load->model('Estado_model');
             $data['all_estado'] = $this->Estado_model->get_all_estado(); 
              $data['_view'] = 'parada/edit';
              $this->load->view('layouts/main',$data);
          }
  }
  else
  show_error('The parada you are trying to edit does not exist.');
  } catch (Exception $ex) {
    throw new Exception('Parada Controller : Error in edit function - ' . $ex);
  }  
} 
/*
  * Deleting parada
  */
  function remove($parada_id)
   {
    try{
      $parada = $this->Parada_model->get_parada($parada_id);
  // check if the parada exists before trying to delete it
       if(isset($parada['parada_id']))
       {
         $this->Parada_model->delete_parada($parada_id);
             $this->session->set_flashdata('alert_msg','<div class="alert alert-success text-center">Succesfully Removed.</div>');
           redirect('parada/index');
       }
       else
       show_error('The parada you are trying to delete does not exist.');
  } catch (Exception $ex) {
    throw new Exception('Parada Controller : Error in remove function - ' . $ex);
  }  
  }
  /*
  * View more a parada
 */
 public function view_more($parada_id)
 {   
     $data['sistema'] = $this->sistema;
try{
   $data['parada'] = $this->Parada_model->get_parada($parada_id);
     if(isset($data['parada']['parada_id']))
      {
              $data['_view'] = 'parada/view_more';
              $this->load->view('layouts/main',$data);
      }
      else
        show_error('The parada you are trying to view more does not exist.');
    } catch (Exception $ex) {
    throw new Exception('Parada Controller : Error in View more function - ' . $ex);
  }  
} 
 /*
* Listing of parada
 */
public function search_by_clm()
{
    $column_name= $this->input->post('column_name');
    $value_id= $this->input->post('value_id');
     $data['noof_page'] = 0;
     $params = array();
    $data['parada'] = $this->Parada_model->get_all_with_asso_parada_by_cat($column_name,$value_id);
      $data['_view'] = 'parada/index';
      $this->load->view('layouts/main',$data);
}
  /*
  * get get_search_values_byclms by id
 */
 public function get_search_values_by_estado_id()
 {   
     $data['sistema'] = $this->sistema;
     $estado_id= $this->input->post('value');
        $this->load->model('Estado_model');
        $data['all_estado'] = $this->Estado_model->get_all_estado(); 
      $parada = $this->Parada_model->get_all_with_asso_parada();
if(isset($data['all_estado']) && $data['all_estado']!=null)
                                              {
                                              foreach($data['all_estado'] as $p){ 
      echo          "<option value='".$p['estado_id']."'> ".$p['estado_descripcion']."</option>"; 
 } 
                                              }
                                              else{
                                                        echo '<tr>No data found</tr>';
                                              }
 } 
 /*
* get search values by column- parada
 */
public function get_search_values_by_clm()
{
    $clm_name= $this->input->post('clm_name');
    $value= $this->input->post('value');
    $id= $this->input->post('id');
        $params = array(
        $clm_name=>$value,
        );
           $this->Parada_model->update_parada($id,$params);
   $data['noof_page'] = 0;
  $data['parada'] = $this->Parada_model->get_all_with_asso_parada();
  $this->load->view('parada/index',$data);
}
 }
