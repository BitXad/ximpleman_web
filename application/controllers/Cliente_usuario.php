<?php
/* 
 * Generated by CRUDigniter v3.2 
 * www.crudigniter.com
 */
 
class Cliente_usuario extends CI_Controller{
    private $sistema;
    
    function __construct()
    {
        parent::__construct();
        $this->load->model('Cliente_usuario_model');
        $this->load->model('Sistema_model');
        $this->sistema = $this->Sistema_model->get_sistema();
    } 

    /*
     * Listing of cliente_usuario
     */
    function index()
    {
        $data['sistema'] = $this->sistema;
        $params['limit'] = RECORDS_PER_PAGE; 
        $params['offset'] = ($this->input->get('per_page')) ? $this->input->get('per_page') : 0;
        
        $config = $this->config->item('pagination');
        $config['base_url'] = site_url('cliente_usuario/index?');
        $config['total_rows'] = $this->Cliente_usuario_model->get_all_cliente_usuario_count();
        $this->pagination->initialize($config);

        $data['cliente_usuario'] = $this->Cliente_usuario_model->get_all_cliente_usuario($params);
        
        $data['_view'] = 'cliente_usuario/index';
        $this->load->view('layouts/main',$data);
    }

    /*
     * Adding a new cliente_usuario
     */
    function add()
    {
        $data['sistema'] = $this->sistema;
        if(isset($_POST) && count($_POST) > 0)     
        {   
            $params = array(
            );
            
            $cliente_usuario_id = $this->Cliente_usuario_model->add_cliente_usuario($params);
            redirect('cliente_usuario/index');
        }
        else
        {            
            $data['_view'] = 'cliente_usuario/add';
            $this->load->view('layouts/main',$data);
        }
    }  

    /*
     * Editing a cliente_usuario
     */
    function edit($cliente_id)
    {
        $data['sistema'] = $this->sistema;
        // check if the cliente_usuario exists before trying to edit it
        $data['cliente_usuario'] = $this->Cliente_usuario_model->get_cliente_usuario($cliente_id);
        
        if(isset($data['cliente_usuario']['cliente_id']))
        {
            if(isset($_POST) && count($_POST) > 0)     
            {   
                $params = array(
                );

                $this->Cliente_usuario_model->update_cliente_usuario($cliente_id,$params);            
                redirect('cliente_usuario/index');
            }
            else
            {
                $data['_view'] = 'cliente_usuario/edit';
                $this->load->view('layouts/main',$data);
            }
        }
        else
            show_error('The cliente_usuario you are trying to edit does not exist.');
    } 

    /*
     * Deleting cliente_usuario
     */
    function remove($cliente_id)
    {
        $data['sistema'] = $this->sistema;
        $cliente_usuario = $this->Cliente_usuario_model->get_cliente_usuario($cliente_id);

        // check if the cliente_usuario exists before trying to delete it
        if(isset($cliente_usuario['cliente_id']))
        {
            $this->Cliente_usuario_model->delete_cliente_usuario($cliente_id);
            redirect('cliente_usuario/index');
        }
        else
            show_error('The cliente_usuario you are trying to delete does not exist.');
    }
    
}
