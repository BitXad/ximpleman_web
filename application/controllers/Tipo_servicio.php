<?php
/* 
 * Generated by CRUDigniter v3.2 
 * www.crudigniter.com
 */
 
class Tipo_servicio extends CI_Controller{
    function __construct()
    {
        parent::__construct();
        $this->load->model('Tipo_servicio_model');
    } 
    
    /*
     * Listing of tipo_servicio
     */
    function index()
    {
        if ($this->session->userdata('logged_in')) {
            $session_data = $this->session->userdata('logged_in');
            if($session_data['tipousuario_id']==1) {
                $data = array(
                    'page_title' => 'Admin >> Mi Cuenta'
                );
        $data['tipo_servicio'] = $this->Tipo_servicio_model->get_all_tipo_servicio();
        
        $data['_view'] = 'tipo_servicio/index';
        $this->load->view('layouts/main',$data);
        }
            else{
                redirect('alerta');
            }
        } else {
            redirect('', 'refresh');
        }
    }

    /*
     * Adding a new tipo_servicio
     */
    function add()
    {
        if ($this->session->userdata('logged_in')) {
            $session_data = $this->session->userdata('logged_in');
            if($session_data['tipousuario_id']==1) {
                $data = array(
                    'page_title' => 'Admin >> Mi Cuenta'
                );
        if(isset($_POST) && count($_POST) > 0)     
        {   
            $params = array(
				'tiposerv_descripcion' => $this->input->post('tiposerv_descripcion'),
				'estado_id' => $this->input->post('estado_id'),
            );
            
            $tipo_servicio_id = $this->Tipo_servicio_model->add_tipo_servicio($params);
            redirect('tipo_servicio/index');
        }
        else
        {
            $this->load->model('Estado_model');
	    $data['all_estado'] = $this->Estado_model->get_all_estado_activo_inactivo();
            $data['_view'] = 'tipo_servicio/add';
            $this->load->view('layouts/main',$data);
        }
        }
            else{
                redirect('alerta');
            }
        } else {
            redirect('', 'refresh');
        }
    }  

    /*
     * Editing a tipo_servicio
     */
    function edit($tiposerv_id)
    {
        if ($this->session->userdata('logged_in')) {
            $session_data = $this->session->userdata('logged_in');
            if($session_data['tipousuario_id']==1) {
                $data = array(
                    'page_title' => 'Admin >> Mi Cuenta'
                );
        // check if the tipo_servicio exists before trying to edit it
        $data['tipo_servicio'] = $this->Tipo_servicio_model->get_tipo_servicio($tiposerv_id);
        
        if(isset($data['tipo_servicio']['tiposerv_id']))
        {
            if(isset($_POST) && count($_POST) > 0)     
            {   
                $params = array(
					'tiposerv_descripcion' => $this->input->post('tiposerv_descripcion'),
					'estado_id' => $this->input->post('estado_id'),
                );

                $this->Tipo_servicio_model->update_tipo_servicio($tiposerv_id,$params);            
                redirect('tipo_servicio/index');
            }
            else
            {
                $this->load->model('Estado_model');
	        $data['all_estado'] = $this->Estado_model->get_all_estado_activo_inactivo();
                $data['_view'] = 'tipo_servicio/edit';
                $this->load->view('layouts/main',$data);
            }
        }
        else
            show_error('The tipo_servicio you are trying to edit does not exist.');
        }
            else{
                redirect('alerta');
            }
        } else {
            redirect('', 'refresh');
        }
    } 

    /*
     * Deleting tipo_servicio
     */
    function remove($tiposerv_id)
    {
        if ($this->session->userdata('logged_in')) {
            $session_data = $this->session->userdata('logged_in');
            if($session_data['tipousuario_id']==1) {
        $tipo_servicio = $this->Tipo_servicio_model->get_tipo_servicio($tiposerv_id);

        // check if the tipo_servicio exists before trying to delete it
        if(isset($tipo_servicio['tiposerv_id']))
        {
            $this->Tipo_servicio_model->delete_tipo_servicio($tiposerv_id);
            redirect('tipo_servicio/index');
        }
        else
            show_error('The tipo_servicio you are trying to delete does not exist.');
        }
            else{
                redirect('alerta');
            }
        } else {
            redirect('', 'refresh');
        }
    }
    
}
