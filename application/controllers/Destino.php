<?php 
/*
 Generated by Manuigniter v2.0 
 www.manuigniter.com
*/
class Destino extends CI_Controller{

    private $sistema;
    private $parametros;

 function __construct()
 {
       parent::__construct();
      $this->load->model('Destino_model');
      
        $this->load->model('Sistema_model');
        $this->load->model('Parametro_model');
        $this->sistema = $this->Sistema_model->get_sistema();
        $parametro = $this->Parametro_model->get_parametros();
        $this->parametros = $parametro[0];
        
        
        if ($this->session->userdata('logged_in')) {
            $this->session_data = $this->session->userdata('logged_in');
        }else {
            redirect('', 'refresh');
        }
 } 
 /*
* Listing of destino
 */
public function index()
{
     $data['sistema'] = $this->sistema;
  try{
      $data['noof_page'] = 0;
     $data['destino'] = $this->Destino_model->get_all_destino();
      $data['_view'] = 'destino/index';
      $this->load->view('layouts/main',$data);
    } catch (Exception $ex) {
      throw new Exception('Destino Controller : Error in index function - ' . $ex);
  }  
}
 /*
  * Adding a new destino
  */
 function add()
 {  
     $data['sistema'] = $this->sistema;
try{
      $params = array(
       'destino_nombre'=> $this->input->post('destino_nombre'),
       'destino_ubicacion'=> $this->input->post('destino_ubicacion'),
        );
       $this->load->library('upload');
       $this->load->library('form_validation');
       if(isset($_POST) && count($_POST) > 0)     
        {  
            $destino_id= $this->Destino_model->add_destino($params);
             $this->session->set_flashdata('alert_msg','<div class="alert alert-success text-center">Succesfully added.</div>');
              redirect('destino/index');
        }
        else
        { 
           $data['_view'] = 'destino/add';
            $this->load->view('layouts/main',$data);
        }
  } catch (Exception $ex) {
    throw new Exception('Destino Controller : Error in add function - ' . $ex);
  }  
 }  
  /*
  * Editing a destino
 */
 public function edit($destino_id)
 {   
     $data['sistema'] = $this->sistema;
  try{
   $data['destino'] = $this->Destino_model->get_destino($destino_id);
       $this->load->library('upload');
       $this->load->library('form_validation');
     if(isset($data['destino']['destino_id']))
      {
        $params = array(
           'destino_nombre'=> $this->input->post('destino_nombre'),
           'destino_ubicacion'=> $this->input->post('destino_ubicacion'),
        );
          if(isset($_POST) && count($_POST) > 0)     
           {  
           $this->Destino_model->update_destino($destino_id,$params);
             $this->session->set_flashdata('alert_msg','<div class="alert alert-success text-center">Succesfully updated.</div>');
                redirect('destino/index');
           }
           else
          {
              $data['_view'] = 'destino/edit';
              $this->load->view('layouts/main',$data);
          }
  }
  else
  show_error('The destino you are trying to edit does not exist.');
  } catch (Exception $ex) {
    throw new Exception('Destino Controller : Error in edit function - ' . $ex);
  }  
} 
/*
  * Deleting destino
  */
  function remove($destino_id)
   {
     $data['sistema'] = $this->sistema;
    try{
      $destino = $this->Destino_model->get_destino($destino_id);
  // check if the destino exists before trying to delete it
       if(isset($destino['destino_id']))
       {
         $this->Destino_model->delete_destino($destino_id);
             $this->session->set_flashdata('alert_msg','<div class="alert alert-success text-center">Succesfully Removed.</div>');
           redirect('destino/index');
       }
       else
       show_error('The destino you are trying to delete does not exist.');
  } catch (Exception $ex) {
    throw new Exception('Destino Controller : Error in remove function - ' . $ex);
  }  
  }
  /*
  * View more a destino
 */
 public function view_more($destino_id)
 {   
     $data['sistema'] = $this->sistema;
try{
   $data['destino'] = $this->Destino_model->get_destino($destino_id);
     if(isset($data['destino']['destino_id']))
      {
              $data['_view'] = 'destino/view_more';
              $this->load->view('layouts/main',$data);
      }
      else
        show_error('The destino you are trying to view more does not exist.');
    } catch (Exception $ex) {
    throw new Exception('Destino Controller : Error in View more function - ' . $ex);
  }  
} 
 /*
* Listing of destino
 */
public function search_by_clm()
{
    $column_name= $this->input->post('column_name');
    $value_id= $this->input->post('value_id');
     $data['noof_page'] = 0;
     $params = array();
    $data['destino'] = $this->Destino_model->get_all_destino_by_cat($column_name,$value_id);
      $data['_view'] = 'destino/index';
      $this->load->view('layouts/main',$data);
}
 /*
* get search values by column- destino
 */
public function get_search_values_by_clm()
{
    $clm_name= $this->input->post('clm_name');
    $value= $this->input->post('value');
    $id= $this->input->post('id');
        $params = array(
        $clm_name=>$value,
        );
           $this->Destino_model->update_destino($id,$params);
   $data['noof_page'] = 0;
  $data['destino'] = $this->Destino_model->get_all_destino();
  $this->load->view('destino/index',$data);
}
 }
