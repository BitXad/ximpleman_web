<?php
/* 
 * Generated by CRUDigniter v3.2 
 * www.crudigniter.com
 */
 
class Detalle_formula extends CI_Controller{
    private $sistema;
    
    function __construct()
    {
        parent::__construct();
        $this->load->model('Detalle_formula_model');
        $this->load->model('Sistema_model');
	$this->sistema = $this->Sistema_model->get_sistema();
    } 

    /*
     * Listing of detalle_formula
     */
    function index()
    {
        $data['sistema'] = $this->sistema;
        $data['detalle_formula'] = $this->Detalle_formula_model->get_all_detalle_formula();
        
        $data['_view'] = 'detalle_formula/index';
        $this->load->view('layouts/main',$data);
    }

    /*
     * Adding a new detalle_formula
     */
    function add()
    {
        $data['sistema'] = $this->sistema;
        if(isset($_POST) && count($_POST) > 0)     
        {   
            $params = array(
				'producto_id' => $this->input->post('producto_id'),
				'formula_id' => $this->input->post('formula_id'),
				'detalleformula_costo' => $this->input->post('detalleformula_costo'),
				'detalleformula_cantidad' => $this->input->post('detalleformula_cantidad'),
            );
            
            $detalle_formula_id = $this->Detalle_formula_model->add_detalle_formula($params);
            redirect('detalle_formula/index');
        }
        else
        {
			$this->load->model('Producto_model');
			$data['all_producto'] = $this->Producto_model->get_all_producto();

			$this->load->model('Formula_model');
			$data['all_formula'] = $this->Formula_model->get_all_formula();
            
            $data['_view'] = 'detalle_formula/add';
            $this->load->view('layouts/main',$data);
        }
    }  

    /*
     * Editing a detalle_formula
     */
    function edit($detalleformula_id)
    {
        $data['sistema'] = $this->sistema;
        // check if the detalle_formula exists before trying to edit it
        $data['detalle_formula'] = $this->Detalle_formula_model->get_detalle_formula($detalleformula_id);
        
        if(isset($data['detalle_formula']['detalleformula_id']))
        {
            if(isset($_POST) && count($_POST) > 0)     
            {   
                $params = array(
					'producto_id' => $this->input->post('producto_id'),
					'formula_id' => $this->input->post('formula_id'),
					'detalleformula_costo' => $this->input->post('detalleformula_costo'),
					'detalleformula_cantidad' => $this->input->post('detalleformula_cantidad'),
                );

                $this->Detalle_formula_model->update_detalle_formula($detalleformula_id,$params);            
                redirect('detalle_formula/index');
            }
            else
            {
				$this->load->model('Producto_model');
				$data['all_producto'] = $this->Producto_model->get_all_producto();

				$this->load->model('Formula_model');
				$data['all_formula'] = $this->Formula_model->get_all_formula();

                $data['_view'] = 'detalle_formula/edit';
                $this->load->view('layouts/main',$data);
            }
        }
        else
            show_error('The detalle_formula you are trying to edit does not exist.');
    } 

    /*
     * Deleting detalle_formula
     */
    function remove($detalleformula_id)
    {
        $data['sistema'] = $this->sistema;
        $detalle_formula = $this->Detalle_formula_model->get_detalle_formula($detalleformula_id);

        // check if the detalle_formula exists before trying to delete it
        if(isset($detalle_formula['detalleformula_id']))
        {
            $this->Detalle_formula_model->delete_detalle_formula($detalleformula_id);
            redirect('detalle_formula/index');
        }
        else
            show_error('The detalle_formula you are trying to delete does not exist.');
    }
    
}
