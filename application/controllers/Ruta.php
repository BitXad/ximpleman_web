<?php 
/*
 Generated by Manuigniter v2.0 
 www.manuigniter.com
*/
class Ruta extends CI_Controller{

    private $sistema;
    private $parametros;

 function __construct()
 {
       parent::__construct();
      $this->load->model('Ruta_model');
      
        $this->load->model('Sistema_model');
        $this->load->model('Parametro_model');
        $this->sistema = $this->Sistema_model->get_sistema();
        $parametro = $this->Parametro_model->get_parametros();
        $this->parametros = $parametro[0];
        
        
        if ($this->session->userdata('logged_in')) {
            $this->session_data = $this->session->userdata('logged_in');
        }else {
            redirect('', 'refresh');
        }
 } 
 /*
* Listing of ruta
 */
public function index()
{
     $data['sistema'] = $this->sistema;
  try{
      $data['noof_page'] = 0;
     $data['ruta'] = $this->Ruta_model->get_all_with_asso_ruta();
      $data['_view'] = 'ruta/index';
      $this->load->view('layouts/main',$data);
    } catch (Exception $ex) {
      throw new Exception('Ruta Controller : Error in index function - ' . $ex);
  }  
}
 /*
  * Adding a new ruta
  */
 function add()
 {  
     $data['sistema'] = $this->sistema;
try{
      $params = array(
       'parada_id'=> $this->input->post('parada_id'),
       'destino_id'=> $this->input->post('destino_id'),
       'origen_id'=> $this->input->post('origen_id'),
       'ruta_nombre'=> $this->input->post('ruta_nombre'),
       'ruta_descripcion'=> $this->input->post('ruta_descripcion'),
       'inicio_ruta'=> $this->input->post('inicio_ruta'),
       'fin_ruta'=> $this->input->post('fin_ruta'),
        );
       $this->load->library('upload');
       $this->load->library('form_validation');
       if(isset($_POST) && count($_POST) > 0)     
        {  
            $ruta_id= $this->Ruta_model->add_ruta($params);
             $this->session->set_flashdata('alert_msg','<div class="alert alert-success text-center">Succesfully added.</div>');
              redirect('ruta/index');
        }
        else
        { 
         $this->load->model('Parada_model');
         $data['all_parada'] = $this->Parada_model->get_all_parada(); 
         $this->load->model('Origen_model');
         $data['all_origen'] = $this->Origen_model->get_all_origen(); 
           $data['_view'] = 'ruta/add';
            $this->load->view('layouts/main',$data);
        }
  } catch (Exception $ex) {
    throw new Exception('Ruta Controller : Error in add function - ' . $ex);
  }  
 }  
  /*
  * Editing a ruta
 */
 public function edit($ruta_id)
 {   
     $data['sistema'] = $this->sistema;
  try{
   $data['ruta'] = $this->Ruta_model->get_ruta($ruta_id);
       $this->load->library('upload');
       $this->load->library('form_validation');
     if(isset($data['ruta']['ruta_id']))
      {
        $params = array(
           'parada_id'=> $this->input->post('parada_id'),
           'destino_id'=> $this->input->post('destino_id'),
           'origen_id'=> $this->input->post('origen_id'),
           'ruta_nombre'=> $this->input->post('ruta_nombre'),
           'ruta_descripcion'=> $this->input->post('ruta_descripcion'),
           'inicio_ruta'=> $this->input->post('inicio_ruta'),
           'fin_ruta'=> $this->input->post('fin_ruta'),
        );
          if(isset($_POST) && count($_POST) > 0)     
           {  
           $this->Ruta_model->update_ruta($ruta_id,$params);
             $this->session->set_flashdata('alert_msg','<div class="alert alert-success text-center">Succesfully updated.</div>');
                redirect('ruta/index');
           }
           else
          {
             $this->load->model('Parada_model');
             $data['all_parada'] = $this->Parada_model->get_all_parada(); 
             $this->load->model('Origen_model');
             $data['all_origen'] = $this->Origen_model->get_all_origen(); 
              $data['_view'] = 'ruta/edit';
              $this->load->view('layouts/main',$data);
          }
  }
  else
  show_error('The ruta you are trying to edit does not exist.');
  } catch (Exception $ex) {
    throw new Exception('Ruta Controller : Error in edit function - ' . $ex);
  }  
} 
/*
  * Deleting ruta
  */
  function remove($ruta_id)
   {
     $data['sistema'] = $this->sistema;
    try{
      $ruta = $this->Ruta_model->get_ruta($ruta_id);
  // check if the ruta exists before trying to delete it
       if(isset($ruta['ruta_id']))
       {
         $this->Ruta_model->delete_ruta($ruta_id);
             $this->session->set_flashdata('alert_msg','<div class="alert alert-success text-center">Succesfully Removed.</div>');
           redirect('ruta/index');
       }
       else
       show_error('The ruta you are trying to delete does not exist.');
  } catch (Exception $ex) {
    throw new Exception('Ruta Controller : Error in remove function - ' . $ex);
  }  
  }
  /*
  * View more a ruta
 */
 public function view_more($ruta_id)
 {   
     $data['sistema'] = $this->sistema;
try{
   $data['ruta'] = $this->Ruta_model->get_ruta($ruta_id);
     if(isset($data['ruta']['ruta_id']))
      {
              $data['_view'] = 'ruta/view_more';
              $this->load->view('layouts/main',$data);
      }
      else
        show_error('The ruta you are trying to view more does not exist.');
    } catch (Exception $ex) {
    throw new Exception('Ruta Controller : Error in View more function - ' . $ex);
  }  
} 
 /*
* Listing of ruta
 */
public function search_by_clm()
{
    $column_name= $this->input->post('column_name');
    $value_id= $this->input->post('value_id');
     $data['noof_page'] = 0;
     $params = array();
    $data['ruta'] = $this->Ruta_model->get_all_with_asso_ruta_by_cat($column_name,$value_id);
      $data['_view'] = 'ruta/index';
      $this->load->view('layouts/main',$data);
}
  /*
  * get get_search_values_byclms by id
 */
 public function get_search_values_by_parada_id()
 {   
     $parada_id= $this->input->post('value');
        $this->load->model('Parada_model');
        $data['all_parada'] = $this->Parada_model->get_all_parada(); 
      $ruta = $this->Ruta_model->get_all_with_asso_ruta();
if(isset($data['all_parada']) && $data['all_parada']!=null)
                                              {
                                              foreach($data['all_parada'] as $r){ 
      echo          "<option value='".$r['parada_id']."'> ".$r['parada_nombre']."</option>"; 
 } 
                                              }
                                              else{
                                                        echo '<tr>No data found</tr>';
                                              }
 } 
  /*
  * get get_search_values_byclms by id
 */
 public function get_search_values_by_destino_id()
 {   
     $destino_id= $this->input->post('value');
      $ruta = $this->Ruta_model->get_all_with_asso_ruta();
if(isset($data['all_parada']) && $data['all_parada']!=null)
                                              {
                                              foreach($data['all_parada'] as $r){ 
      echo          "<option value='".$r['']."'> ".$r['']."</option>"; 
 } 
                                              }
                                              else{
                                                        echo '<tr>No data found</tr>';
                                              }
 } 
  /*
  * get get_search_values_byclms by id
 */
 public function get_search_values_by_origen_id()
 {   
     $origen_id= $this->input->post('value');
        $this->load->model('Origen_model');
        $data['all_origen'] = $this->Origen_model->get_all_origen(); 
      $ruta = $this->Ruta_model->get_all_with_asso_ruta();
if(isset($data['all_origen']) && $data['all_origen']!=null)
                                              {
                                              foreach($data['all_origen'] as $r){ 
      echo          "<option value='".$r['origen_id']."'> ".$r['origen_nombre']."</option>"; 
 } 
                                              }
                                              else{
                                                        echo '<tr>No data found</tr>';
                                              }
 } 
 /*
* get search values by column- ruta
 */
public function get_search_values_by_clm()
{
    $clm_name= $this->input->post('clm_name');
    $value= $this->input->post('value');
    $id= $this->input->post('id');
        $params = array(
        $clm_name=>$value,
        );
           $this->Ruta_model->update_ruta($id,$params);
   $data['noof_page'] = 0;
  $data['ruta'] = $this->Ruta_model->get_all_with_asso_ruta();
  $this->load->view('ruta/index',$data);
}
 }
