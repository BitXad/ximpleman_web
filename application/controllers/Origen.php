<?php 
/*
 Generated by Manuigniter v2.0 
 www.manuigniter.com
*/
class Origen extends CI_Controller{

    private $sistema;
    private $parametros;

 function __construct()
 {
       parent::__construct();
      $this->load->model('Origen_model');
      
        $this->load->model('Sistema_model');
        $this->load->model('Parametro_model');
        $this->sistema = $this->Sistema_model->get_sistema();
        $parametro = $this->Parametro_model->get_parametros();
        $this->parametros = $parametro[0];
        
        
        if ($this->session->userdata('logged_in')) {
            $this->session_data = $this->session->userdata('logged_in');
        }else {
            redirect('', 'refresh');
        }
 } 
 /*
* Listing of origen
 */
public function index()
{
     $data['sistema'] = $this->sistema;
  try{
      $data['noof_page'] = 0;
     $data['origen'] = $this->Origen_model->get_all_origen();
      $data['_view'] = 'origen/index';
      $this->load->view('layouts/main',$data);
    } catch (Exception $ex) {
      throw new Exception('Origen Controller : Error in index function - ' . $ex);
  }  
}
 /*
  * Adding a new origen
  */
 function add()
 {  
     $data['sistema'] = $this->sistema;
try{
      $params = array(
       'origen_nombre'=> $this->input->post('origen_nombre'),
       'origen_ubicacion'=> $this->input->post('origen_ubicacion'),
        );
       $this->load->library('upload');
       $this->load->library('form_validation');
       if(isset($_POST) && count($_POST) > 0)     
        {  
            $origen_id= $this->Origen_model->add_origen($params);
             $this->session->set_flashdata('alert_msg','<div class="alert alert-success text-center">Succesfully added.</div>');
              redirect('origen/index');
        }
        else
        { 
           $data['_view'] = 'origen/add';
            $this->load->view('layouts/main',$data);
        }
  } catch (Exception $ex) {
    throw new Exception('Origen Controller : Error in add function - ' . $ex);
  }  
 }  
  /*
  * Editing a origen
 */
 public function edit($origen_id)
 {   
     $data['sistema'] = $this->sistema;
  try{
   $data['origen'] = $this->Origen_model->get_origen($origen_id);
       $this->load->library('upload');
       $this->load->library('form_validation');
     if(isset($data['origen']['origen_id']))
      {
        $params = array(
           'origen_nombre'=> $this->input->post('origen_nombre'),
           'origen_ubicacion'=> $this->input->post('origen_ubicacion'),
        );
          if(isset($_POST) && count($_POST) > 0)     
           {  
           $this->Origen_model->update_origen($origen_id,$params);
             $this->session->set_flashdata('alert_msg','<div class="alert alert-success text-center">Succesfully updated.</div>');
                redirect('origen/index');
           }
           else
          {
              $data['_view'] = 'origen/edit';
              $this->load->view('layouts/main',$data);
          }
  }
  else
  show_error('The origen you are trying to edit does not exist.');
  } catch (Exception $ex) {
    throw new Exception('Origen Controller : Error in edit function - ' . $ex);
  }  
} 
/*
  * Deleting origen
  */
  function remove($origen_id)
   {
     $data['sistema'] = $this->sistema;
    try{
      $origen = $this->Origen_model->get_origen($origen_id);
  // check if the origen exists before trying to delete it
       if(isset($origen['origen_id']))
       {
         $this->Origen_model->delete_origen($origen_id);
             $this->session->set_flashdata('alert_msg','<div class="alert alert-success text-center">Succesfully Removed.</div>');
           redirect('origen/index');
       }
       else
       show_error('The origen you are trying to delete does not exist.');
  } catch (Exception $ex) {
    throw new Exception('Origen Controller : Error in remove function - ' . $ex);
  }  
  }
  /*
  * View more a origen
 */
 public function view_more($origen_id)
 {   
try{
   $data['origen'] = $this->Origen_model->get_origen($origen_id);
     if(isset($data['origen']['origen_id']))
      {
              $data['_view'] = 'origen/view_more';
              $this->load->view('layouts/main',$data);
      }
      else
        show_error('The origen you are trying to view more does not exist.');
    } catch (Exception $ex) {
    throw new Exception('Origen Controller : Error in View more function - ' . $ex);
  }  
} 
 /*
* Listing of origen
 */
public function search_by_clm()
{
    $column_name= $this->input->post('column_name');
    $value_id= $this->input->post('value_id');
     $data['noof_page'] = 0;
     $params = array();
    $data['origen'] = $this->Origen_model->get_all_origen_by_cat($column_name,$value_id);
      $data['_view'] = 'origen/index';
      $this->load->view('layouts/main',$data);
}
 /*
* get search values by column- origen
 */
public function get_search_values_by_clm()
{
    $clm_name= $this->input->post('clm_name');
    $value= $this->input->post('value');
    $id= $this->input->post('id');
        $params = array(
        $clm_name=>$value,
        );
           $this->Origen_model->update_origen($id,$params);
   $data['noof_page'] = 0;
  $data['origen'] = $this->Origen_model->get_all_origen();
  $this->load->view('origen/index',$data);
}
 }
