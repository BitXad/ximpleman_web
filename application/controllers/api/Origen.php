<?php 
/*
    Generated by Manuigniter v2.0 
    www.manuigniter.com
*/
use Restserver\Libraries\REST_Controller;
defined('BASEPATH') OR exit('No direct script access allowed');
require APPPATH . '/libraries/REST_Controller.php';
require APPPATH . 'libraries/Format.php';
class Origen extends REST_Controller{
 function __construct()
 {
       parent::__construct();
      $this->load->model('Origen_model');
 } 
 /*
* Listing of origen
 */
public function get_all_post()
{
  try{
  $data['origen'] = $this->Origen_model->get_all_origen();
     if($data['origen'] && $data['origen']!=null){
       $origen_ar = array();
       foreach($data['origen'] as $o)
       {
       $o_ar = array();
       $o_ar['origen_id'] = $o['origen_id'];
       $o_ar['origen_nombre'] = $o['origen_nombre'];
       $o_ar['origen_ubicacion'] = $o['origen_ubicacion'];
       $origen_ar[] = $o_ar;
       }
       $response = array(
       'status' => 200,
       'message' => 'get all data Succesfully',
       'data' => $origen_ar,
       );
       $this->response($response, REST_Controller::HTTP_OK);
     }
     else{
       $response = array(
      'status' => 400,
      'message' => 'Detail is not found'
        );
       $this->response($response, REST_Controller::HTTP_OK); 
        }
       } catch (Exception $ex) {
             throw new Exception('Origen controller_name : Error in get_all_post function - ' . $ex);
        }  
}
 /*
  * Adding a new origen
  */
 function addnew_post()
 {  
  try{
      $params = array(
       'origen_nombre'=> $this->input->post('origen_nombre'),
       'origen_ubicacion'=> $this->input->post('origen_ubicacion'),
        );
       $this->load->library('upload');
       $this->load->library('form_validation');
       if(isset($_POST) && count($_POST) > 0)     
        {  
            $origen_id= $this->Origen_model->add_origen($params);
   $data['origen'] = $this->Origen_model->get_origen($origen_id);
           $response = array(
            'status' => 200,
            'message' => 'Succesfully Added',
            'data' => $data
             );
           $this->response($response, REST_Controller::HTTP_OK);
        }
        else
        { 
           $response = array(
            'status' => 400,
            'message' => 'Not Added try again',
            'data' => ''
             );
           $this->response($response, REST_Controller::HTTP_OK);
        }
       } catch (Exception $ex) {
             throw new Exception('Origen controller_name : Error in new origen function - ' . $ex);
       }  
 }  
  /*
  * Editing a origen
 */
  function edit_post($origen_id)
 {   
  try{
   $data['origen'] = $this->Origen_model->get_origen($origen_id);
       $this->load->library('upload');
       $this->load->library('form_validation');
     if(isset($data['origen']['origen_id']))
      {
        $params = array(
           'origen_nombre'=> $this->input->post('origen_nombre'),
           'origen_ubicacion'=> $this->input->post('origen_ubicacion'),
        );
          if(isset($_POST) && count($_POST) > 0)     
           {  
           $this->Origen_model->update_origen($origen_id,$params);
           $response = array(
            'status' => 200,
            'message' => 'Succesfully Updated',
            'data' => $origen_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
           }
           else
          {
           $response = array(
            'status' => 400,
            'message' => 'Not Updated Try again',
            'data' => $origen_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
          }
  }
       } catch (Exception $ex) {
             throw new Exception('Origen controller_name : Error in edit_post function - ' . $ex);
        }  
} 
/*
  * Deleting origen
  */
  function remove_post($origen_id)
   {
  try{
      $origen = $this->Origen_model->get_origen($origen_id);
  // check if the origen exists before trying to delete it
       if(isset($origen['origen_id']))
       {
         $this->Origen_model->delete_origen($origen_id);
           $response = array(
            'status' => 200,
            'message' => 'Succesfully Removed',
            'data' => $origen_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
       }
       else
           $response = array(
            'status' => 400,
            'message' => 'Not Updated Try again',
            'data' => $origen_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
       } catch (Exception $ex) {
             throw new Exception('Origen controller_name : Error in remove_post function - ' . $ex);
        }  
  }
 }
