<?php 
/*
    Generated by Manuigniter v2.0 
    www.manuigniter.com
*/
use Restserver\Libraries\REST_Controller;
defined('BASEPATH') OR exit('No direct script access allowed');
require APPPATH . '/libraries/REST_Controller.php';
require APPPATH . 'libraries/Format.php';
class Viaje extends REST_Controller{
 function __construct()
 {
       parent::__construct();
      $this->load->model('Viaje_model');
 } 
 /*
* Listing of viaje
 */
public function get_all_post()
{
  try{
  $data['viaje'] = $this->Viaje_model->get_all_viaje();
     if($data['viaje'] && $data['viaje']!=null){
       $viaje_ar = array();
       foreach($data['viaje'] as $v)
       {
       $v_ar = array();
       $v_ar['ayudante_id'] = $v['ayudante_id'];
       $v_ar['viaje_id'] = $v['viaje_id'];
       $v_ar['vehiculo_id'] = $v['vehiculo_id'];
       $v_ar['ruta_id'] = $v['ruta_id'];
       $v_ar['usuario_id'] = $v['usuario_id'];
       $v_ar['conductor_id'] = $v['conductor_id'];
       $v_ar['viaje_fechasalida'] = $v['viaje_fechasalida'];
       $v_ar['viaje_horasalida'] = $v['viaje_horasalida'];
       $v_ar['viaje_fechallegada'] = $v['viaje_fechallegada'];
       $v_ar['viaje_horallegada'] = $v['viaje_horallegada'];
       $viaje_ar[] = $v_ar;
       }
       $response = array(
       'status' => 200,
       'message' => 'get all data Succesfully',
       'data' => $viaje_ar,
       );
       $this->response($response, REST_Controller::HTTP_OK);
     }
     else{
       $response = array(
      'status' => 400,
      'message' => 'Detail is not found'
        );
       $this->response($response, REST_Controller::HTTP_OK); 
        }
       } catch (Exception $ex) {
             throw new Exception('Viaje controller_name : Error in get_all_post function - ' . $ex);
        }  
}
 /*
  * Adding a new viaje
  */
 function addnew_post()
 {  
  try{
      $params = array(
       'ayudante_id'=> $this->input->post('ayudante_id'),
       'vehiculo_id'=> $this->input->post('vehiculo_id'),
       'ruta_id'=> $this->input->post('ruta_id'),
       'usuario_id'=> $this->input->post('usuario_id'),
       'conductor_id'=> $this->input->post('conductor_id'),
       'viaje_fechasalida'=> $this->input->post('viaje_fechasalida'),
       'viaje_horasalida'=> $this->input->post('viaje_horasalida'),
       'viaje_fechallegada'=> $this->input->post('viaje_fechallegada'),
       'viaje_horallegada'=> $this->input->post('viaje_horallegada'),
        );
       $this->load->library('upload');
       $this->load->library('form_validation');
       if(isset($_POST) && count($_POST) > 0)     
        {  
            $viaje_id= $this->Viaje_model->add_viaje($params);
   $data['viaje'] = $this->Viaje_model->get_viaje($viaje_id);
           $response = array(
            'status' => 200,
            'message' => 'Succesfully Added',
            'data' => $data
             );
           $this->response($response, REST_Controller::HTTP_OK);
        }
        else
        { 
           $response = array(
            'status' => 400,
            'message' => 'Not Added try again',
            'data' => ''
             );
           $this->response($response, REST_Controller::HTTP_OK);
        }
       } catch (Exception $ex) {
             throw new Exception('Viaje controller_name : Error in new viaje function - ' . $ex);
       }  
 }  
  /*
  * Editing a viaje
 */
  function edit_post($viaje_id)
 {   
  try{
   $data['viaje'] = $this->Viaje_model->get_viaje($viaje_id);
       $this->load->library('upload');
       $this->load->library('form_validation');
     if(isset($data['viaje']['viaje_id']))
      {
        $params = array(
           'ayudante_id'=> $this->input->post('ayudante_id'),
           'vehiculo_id'=> $this->input->post('vehiculo_id'),
           'ruta_id'=> $this->input->post('ruta_id'),
           'usuario_id'=> $this->input->post('usuario_id'),
           'conductor_id'=> $this->input->post('conductor_id'),
           'viaje_fechasalida'=> $this->input->post('viaje_fechasalida'),
           'viaje_horasalida'=> $this->input->post('viaje_horasalida'),
           'viaje_fechallegada'=> $this->input->post('viaje_fechallegada'),
           'viaje_horallegada'=> $this->input->post('viaje_horallegada'),
        );
          if(isset($_POST) && count($_POST) > 0)     
           {  
           $this->Viaje_model->update_viaje($viaje_id,$params);
           $response = array(
            'status' => 200,
            'message' => 'Succesfully Updated',
            'data' => $viaje_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
           }
           else
          {
           $response = array(
            'status' => 400,
            'message' => 'Not Updated Try again',
            'data' => $viaje_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
          }
  }
       } catch (Exception $ex) {
             throw new Exception('Viaje controller_name : Error in edit_post function - ' . $ex);
        }  
} 
/*
  * Deleting viaje
  */
  function remove_post($viaje_id)
   {
  try{
      $viaje = $this->Viaje_model->get_viaje($viaje_id);
  // check if the viaje exists before trying to delete it
       if(isset($viaje['viaje_id']))
       {
         $this->Viaje_model->delete_viaje($viaje_id);
           $response = array(
            'status' => 200,
            'message' => 'Succesfully Removed',
            'data' => $viaje_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
       }
       else
           $response = array(
            'status' => 400,
            'message' => 'Not Updated Try again',
            'data' => $viaje_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
       } catch (Exception $ex) {
             throw new Exception('Viaje controller_name : Error in remove_post function - ' . $ex);
        }  
  }
 }
