<?php 
/*
    Generated by Manuigniter v2.0 
    www.manuigniter.com
*/
use Restserver\Libraries\REST_Controller;
defined('BASEPATH') OR exit('No direct script access allowed');
require APPPATH . '/libraries/REST_Controller.php';
require APPPATH . 'libraries/Format.php';
class Ruta extends REST_Controller{
 function __construct()
 {
       parent::__construct();
      $this->load->model('Ruta_model');
 } 
 /*
* Listing of ruta
 */
public function get_all_post()
{
  try{
  $data['ruta'] = $this->Ruta_model->get_all_ruta();
     if($data['ruta'] && $data['ruta']!=null){
       $ruta_ar = array();
       foreach($data['ruta'] as $r)
       {
       $r_ar = array();
       $r_ar['ruta_id'] = $r['ruta_id'];
       $r_ar['parada_id'] = $r['parada_id'];
       $r_ar['destino_id'] = $r['destino_id'];
       $r_ar['origen_id'] = $r['origen_id'];
       $r_ar['ruta_nombre'] = $r['ruta_nombre'];
       $r_ar['ruta_descripcion'] = $r['ruta_descripcion'];
       $r_ar['inicio_ruta'] = $r['inicio_ruta'];
       $r_ar['fin_ruta'] = $r['fin_ruta'];
       $ruta_ar[] = $r_ar;
       }
       $response = array(
       'status' => 200,
       'message' => 'get all data Succesfully',
       'data' => $ruta_ar,
       );
       $this->response($response, REST_Controller::HTTP_OK);
     }
     else{
       $response = array(
      'status' => 400,
      'message' => 'Detail is not found'
        );
       $this->response($response, REST_Controller::HTTP_OK); 
        }
       } catch (Exception $ex) {
             throw new Exception('Ruta controller_name : Error in get_all_post function - ' . $ex);
        }  
}
 /*
  * Adding a new ruta
  */
 function addnew_post()
 {  
  try{
      $params = array(
       'parada_id'=> $this->input->post('parada_id'),
       'destino_id'=> $this->input->post('destino_id'),
       'origen_id'=> $this->input->post('origen_id'),
       'ruta_nombre'=> $this->input->post('ruta_nombre'),
       'ruta_descripcion'=> $this->input->post('ruta_descripcion'),
       'inicio_ruta'=> $this->input->post('inicio_ruta'),
       'fin_ruta'=> $this->input->post('fin_ruta'),
        );
       $this->load->library('upload');
       $this->load->library('form_validation');
       if(isset($_POST) && count($_POST) > 0)     
        {  
            $ruta_id= $this->Ruta_model->add_ruta($params);
   $data['ruta'] = $this->Ruta_model->get_ruta($ruta_id);
           $response = array(
            'status' => 200,
            'message' => 'Succesfully Added',
            'data' => $data
             );
           $this->response($response, REST_Controller::HTTP_OK);
        }
        else
        { 
           $response = array(
            'status' => 400,
            'message' => 'Not Added try again',
            'data' => ''
             );
           $this->response($response, REST_Controller::HTTP_OK);
        }
       } catch (Exception $ex) {
             throw new Exception('Ruta controller_name : Error in new ruta function - ' . $ex);
       }  
 }  
  /*
  * Editing a ruta
 */
  function edit_post($ruta_id)
 {   
  try{
   $data['ruta'] = $this->Ruta_model->get_ruta($ruta_id);
       $this->load->library('upload');
       $this->load->library('form_validation');
     if(isset($data['ruta']['ruta_id']))
      {
        $params = array(
           'parada_id'=> $this->input->post('parada_id'),
           'destino_id'=> $this->input->post('destino_id'),
           'origen_id'=> $this->input->post('origen_id'),
           'ruta_nombre'=> $this->input->post('ruta_nombre'),
           'ruta_descripcion'=> $this->input->post('ruta_descripcion'),
           'inicio_ruta'=> $this->input->post('inicio_ruta'),
           'fin_ruta'=> $this->input->post('fin_ruta'),
        );
          if(isset($_POST) && count($_POST) > 0)     
           {  
           $this->Ruta_model->update_ruta($ruta_id,$params);
           $response = array(
            'status' => 200,
            'message' => 'Succesfully Updated',
            'data' => $ruta_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
           }
           else
          {
           $response = array(
            'status' => 400,
            'message' => 'Not Updated Try again',
            'data' => $ruta_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
          }
  }
       } catch (Exception $ex) {
             throw new Exception('Ruta controller_name : Error in edit_post function - ' . $ex);
        }  
} 
/*
  * Deleting ruta
  */
  function remove_post($ruta_id)
   {
  try{
      $ruta = $this->Ruta_model->get_ruta($ruta_id);
  // check if the ruta exists before trying to delete it
       if(isset($ruta['ruta_id']))
       {
         $this->Ruta_model->delete_ruta($ruta_id);
           $response = array(
            'status' => 200,
            'message' => 'Succesfully Removed',
            'data' => $ruta_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
       }
       else
           $response = array(
            'status' => 400,
            'message' => 'Not Updated Try again',
            'data' => $ruta_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
       } catch (Exception $ex) {
             throw new Exception('Ruta controller_name : Error in remove_post function - ' . $ex);
        }  
  }
 }
