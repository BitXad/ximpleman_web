<?php 
/*
    Generated by Manuigniter v2.0 
    www.manuigniter.com
*/
use Restserver\Libraries\REST_Controller;
defined('BASEPATH') OR exit('No direct script access allowed');
require APPPATH . '/libraries/REST_Controller.php';
require APPPATH . 'libraries/Format.php';
class Asientos extends REST_Controller{
 function __construct()
 {
       parent::__construct();
      $this->load->model('Asientos_model');
 } 
 /*
* Listing of asientos
 */
public function get_all_post()
{
  try{
  $data['asientos'] = $this->Asientos_model->get_all_asientos();
     if($data['asientos'] && $data['asientos']!=null){
       $asientos_ar = array();
       foreach($data['asientos'] as $a)
       {
       $a_ar = array();
       $a_ar['asiento_id'] = $a['asiento_id'];
       $a_ar['nivel_id'] = $a['nivel_id'];
       $a_ar['asiento_numero'] = $a['asiento_numero'];
       $a_ar['asiento_descripcion'] = $a['asiento_descripcion'];
       $a_ar['asiento_caracteristicas'] = $a['asiento_caracteristicas'];
       $a_ar['asiento_foto'] = $a['asiento_foto'];
       $a_ar['asiento_orden'] = $a['asiento_orden'];
       $a_ar['asiento_x'] = $a['asiento_x'];
       $a_ar['asiento_y'] = $a['asiento_y'];
       $a_ar['vehiculo_id'] = $a['vehiculo_id'];
       $asientos_ar[] = $a_ar;
       }
       $response = array(
       'status' => 200,
       'message' => 'get all data Succesfully',
       'data' => $asientos_ar,
       );
       $this->response($response, REST_Controller::HTTP_OK);
     }
     else{
       $response = array(
      'status' => 400,
      'message' => 'Detail is not found'
        );
       $this->response($response, REST_Controller::HTTP_OK); 
        }
       } catch (Exception $ex) {
             throw new Exception('Asientos controller_name : Error in get_all_post function - ' . $ex);
        }  
}
 /*
  * Adding a new asientos
  */
 function addnew_post()
 {  
  try{
      $params = array(
       'nivel_id'=> $this->input->post('nivel_id'),
       'asiento_numero'=> $this->input->post('asiento_numero'),
       'asiento_descripcion'=> $this->input->post('asiento_descripcion'),
       'asiento_caracteristicas'=> $this->input->post('asiento_caracteristicas'),
       'asiento_orden'=> $this->input->post('asiento_orden'),
       'asiento_x'=> $this->input->post('asiento_x'),
       'asiento_y'=> $this->input->post('asiento_y'),
       'vehiculo_id'=> $this->input->post('vehiculo_id'),
        );
       $this->load->library('upload');
       $this->load->library('form_validation');
        $foldername='asiento_foto';
        $fileuploaddire='./resource/'.$foldername;
        $directory=base_url('resource/').$foldername;
        if (!is_dir($fileuploaddire)) {
         mkdir($fileuploaddire, 0777, true);
        }
        $asiento_foto="";
        if (!empty($_FILES['asiento_foto']['name']))
        { 
            $filename =time().$_FILES['asiento_foto']['name'];
            $asiento_foto = str_replace(' ','_',$filename);
            $config['file_name'] = $asiento_foto; 
            $config['upload_path']=$fileuploaddire;
            $config['allowed_types']='pdf|doc|docx|docs|jpg|jpeg|png ';
             $config['max_length']='100000';
            $this->upload->initialize($config);
            if(!$this->upload->do_upload('asiento_foto'))
            {
                $error =$this->upload->display_errors();
                                                    // print_r($error);
                $msg='File Can not Upload ..Please upload pdf |doc |docs|jpg|jpeg|png  type only and Maximum file size 25MB '.$error;
                $this->response(array('status' => 400, 'message' => 'somthing went wrong','image' => $error), REST_Controller::HTTP_OK);
            }
            else 
            {
            }
        }
        $params['asiento_foto'] =$asiento_foto;
       if(isset($_POST) && count($_POST) > 0)     
        {  
            $asiento_id= $this->Asientos_model->add_asientos($params);
   $data['asientos'] = $this->Asientos_model->get_asientos($asiento_id);
           $response = array(
            'status' => 200,
            'message' => 'Succesfully Added',
            'data' => $data
             );
           $this->response($response, REST_Controller::HTTP_OK);
        }
        else
        { 
           $response = array(
            'status' => 400,
            'message' => 'Not Added try again',
            'data' => ''
             );
           $this->response($response, REST_Controller::HTTP_OK);
        }
       } catch (Exception $ex) {
             throw new Exception('Asientos controller_name : Error in new asientos function - ' . $ex);
       }  
 }  
  /*
  * Editing a asientos
 */
  function edit_post($asiento_id)
 {   
  try{
   $data['asientos'] = $this->Asientos_model->get_asientos($asiento_id);
       $this->load->library('upload');
       $this->load->library('form_validation');
     if(isset($data['asientos']['asiento_id']))
      {
        $params = array(
           'nivel_id'=> $this->input->post('nivel_id'),
           'asiento_numero'=> $this->input->post('asiento_numero'),
           'asiento_descripcion'=> $this->input->post('asiento_descripcion'),
           'asiento_caracteristicas'=> $this->input->post('asiento_caracteristicas'),
           'asiento_orden'=> $this->input->post('asiento_orden'),
           'asiento_x'=> $this->input->post('asiento_x'),
           'asiento_y'=> $this->input->post('asiento_y'),
           'vehiculo_id'=> $this->input->post('vehiculo_id'),
        );
      $foldername='asiento_foto';
      $fileuploaddire='./resource/'.$foldername;
      $directory=base_url('resource/').$foldername;
      if (!is_dir($fileuploaddire)) {
         mkdir($fileuploaddire, 0777, true);
      }
      $asiento_foto="";
      if (!empty($_FILES['asiento_foto']['name']))
      { 
          $filename =time().$_FILES['asiento_foto']['name'];
          $asiento_foto = str_replace(' ','_',$filename);
          $config['file_name'] = $asiento_foto; 
          $config['upload_path']=$fileuploaddire;
          $config['allowed_types']='pdf|doc|docx|docs|jpg|jpeg|png ';
          $config['max_length']='100000';
          $this->upload->initialize($config);
          if(!$this->upload->do_upload('asiento_foto'))
          {
              $error =$this->upload->display_errors();
                                                    // print_r($error);
              $msg='File Can not Upload ..Please upload pdf |doc |docs|jpg|jpeg|png  type only and Maximum file size 25MB '.$error;
           $this->response(array('status' => 400,'message' => 'file required'), REST_Controller::HTTP_OK);
              }
              else 
              {
          $pathn=FCPATH.'resource/asiento_foto/'.$data['asientos']['asiento_foto'];
         if(is_file($pathn))
           unlink($pathn);   
                $params['asiento_foto'] =$asiento_foto;
              }
       }
       else
       {
        }
          if(isset($_POST) && count($_POST) > 0)     
           {  
           $this->Asientos_model->update_asientos($asiento_id,$params);
           $response = array(
            'status' => 200,
            'message' => 'Succesfully Updated',
            'data' => $asiento_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
           }
           else
          {
           $response = array(
            'status' => 400,
            'message' => 'Not Updated Try again',
            'data' => $asiento_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
          }
  }
       } catch (Exception $ex) {
             throw new Exception('Asientos controller_name : Error in edit_post function - ' . $ex);
        }  
} 
/*
  * Deleting asientos
  */
  function remove_post($asiento_id)
   {
  try{
      $asientos = $this->Asientos_model->get_asientos($asiento_id);
  // check if the asientos exists before trying to delete it
       if(isset($asientos['asiento_id']))
       {
         $this->Asientos_model->delete_asientos($asiento_id);
          $pathn=FCPATH.'resource/asiento_foto/'.$asientos['asiento_foto'];
         if(is_file($pathn))
           unlink($pathn);   
           $response = array(
            'status' => 200,
            'message' => 'Succesfully Removed',
            'data' => $asiento_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
       }
       else
           $response = array(
            'status' => 400,
            'message' => 'Not Updated Try again',
            'data' => $asiento_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
       } catch (Exception $ex) {
             throw new Exception('Asientos controller_name : Error in remove_post function - ' . $ex);
        }  
  }
 }
