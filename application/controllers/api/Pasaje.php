<?php 
/*
    Generated by Manuigniter v2.0 
    www.manuigniter.com
*/
use Restserver\Libraries\REST_Controller;
defined('BASEPATH') OR exit('No direct script access allowed');
require APPPATH . '/libraries/REST_Controller.php';
require APPPATH . 'libraries/Format.php';
class Pasaje extends REST_Controller{
 function __construct()
 {
       parent::__construct();
      $this->load->model('Pasaje_model');
 } 
 /*
* Listing of pasaje
 */
public function get_all_post()
{
  try{
  $data['pasaje'] = $this->Pasaje_model->get_all_pasaje();
     if($data['pasaje'] && $data['pasaje']!=null){
       $pasaje_ar = array();
       foreach($data['pasaje'] as $p)
       {
       $p_ar = array();
       $p_ar['pasaje_id'] = $p['pasaje_id'];
       $p_ar['factura_id'] = $p['factura_id'];
       $p_ar['asiento_id'] = $p['asiento_id'];
       $p_ar['viaje_id'] = $p['viaje_id'];
       $p_ar['cliente_id'] = $p['cliente_id'];
       $p_ar['pasaje_numero'] = $p['pasaje_numero'];
       $p_ar['pasaje_precio'] = $p['pasaje_precio'];
       $p_ar['pasaje_nombre'] = $p['pasaje_nombre'];
       $p_ar['pasaje_apellido'] = $p['pasaje_apellido'];
       $p_ar['pasaje_fecha'] = $p['pasaje_fecha'];
       $p_ar['pasaje_hora'] = $p['pasaje_hora'];
       $p_ar['estado_id'] = $p['estado_id'];
       $pasaje_ar[] = $p_ar;
       }
       $response = array(
       'status' => 200,
       'message' => 'get all data Succesfully',
       'data' => $pasaje_ar,
       );
       $this->response($response, REST_Controller::HTTP_OK);
     }
     else{
       $response = array(
      'status' => 400,
      'message' => 'Detail is not found'
        );
       $this->response($response, REST_Controller::HTTP_OK); 
        }
       } catch (Exception $ex) {
             throw new Exception('Pasaje controller_name : Error in get_all_post function - ' . $ex);
        }  
}
 /*
  * Adding a new pasaje
  */
 function addnew_post()
 {  
  try{
      $params = array(
       'factura_id'=> $this->input->post('factura_id'),
       'asiento_id'=> $this->input->post('asiento_id'),
       'viaje_id'=> $this->input->post('viaje_id'),
       'cliente_id'=> $this->input->post('cliente_id'),
       'pasaje_numero'=> $this->input->post('pasaje_numero'),
       'pasaje_precio'=> $this->input->post('pasaje_precio'),
       'pasaje_nombre'=> $this->input->post('pasaje_nombre'),
       'pasaje_apellido'=> $this->input->post('pasaje_apellido'),
       'pasaje_fecha'=> $this->input->post('pasaje_fecha'),
       'pasaje_hora'=> $this->input->post('pasaje_hora'),
       'estado_id'=> $this->input->post('estado_id'),
        );
       $this->load->library('upload');
       $this->load->library('form_validation');
       if(isset($_POST) && count($_POST) > 0)     
        {  
            $pasaje_id= $this->Pasaje_model->add_pasaje($params);
   $data['pasaje'] = $this->Pasaje_model->get_pasaje($pasaje_id);
           $response = array(
            'status' => 200,
            'message' => 'Succesfully Added',
            'data' => $data
             );
           $this->response($response, REST_Controller::HTTP_OK);
        }
        else
        { 
           $response = array(
            'status' => 400,
            'message' => 'Not Added try again',
            'data' => ''
             );
           $this->response($response, REST_Controller::HTTP_OK);
        }
       } catch (Exception $ex) {
             throw new Exception('Pasaje controller_name : Error in new pasaje function - ' . $ex);
       }  
 }  
  /*
  * Editing a pasaje
 */
  function edit_post($pasaje_id)
 {   
  try{
   $data['pasaje'] = $this->Pasaje_model->get_pasaje($pasaje_id);
       $this->load->library('upload');
       $this->load->library('form_validation');
     if(isset($data['pasaje']['pasaje_id']))
      {
        $params = array(
           'factura_id'=> $this->input->post('factura_id'),
           'asiento_id'=> $this->input->post('asiento_id'),
           'viaje_id'=> $this->input->post('viaje_id'),
           'cliente_id'=> $this->input->post('cliente_id'),
           'pasaje_numero'=> $this->input->post('pasaje_numero'),
           'pasaje_precio'=> $this->input->post('pasaje_precio'),
           'pasaje_nombre'=> $this->input->post('pasaje_nombre'),
           'pasaje_apellido'=> $this->input->post('pasaje_apellido'),
           'pasaje_fecha'=> $this->input->post('pasaje_fecha'),
           'pasaje_hora'=> $this->input->post('pasaje_hora'),
           'estado_id'=> $this->input->post('estado_id'),
        );
          if(isset($_POST) && count($_POST) > 0)     
           {  
           $this->Pasaje_model->update_pasaje($pasaje_id,$params);
           $response = array(
            'status' => 200,
            'message' => 'Succesfully Updated',
            'data' => $pasaje_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
           }
           else
          {
           $response = array(
            'status' => 400,
            'message' => 'Not Updated Try again',
            'data' => $pasaje_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
          }
  }
       } catch (Exception $ex) {
             throw new Exception('Pasaje controller_name : Error in edit_post function - ' . $ex);
        }  
} 
/*
  * Deleting pasaje
  */
  function remove_post($pasaje_id)
   {
  try{
      $pasaje = $this->Pasaje_model->get_pasaje($pasaje_id);
  // check if the pasaje exists before trying to delete it
       if(isset($pasaje['pasaje_id']))
       {
         $this->Pasaje_model->delete_pasaje($pasaje_id);
           $response = array(
            'status' => 200,
            'message' => 'Succesfully Removed',
            'data' => $pasaje_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
       }
       else
           $response = array(
            'status' => 400,
            'message' => 'Not Updated Try again',
            'data' => $pasaje_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
       } catch (Exception $ex) {
             throw new Exception('Pasaje controller_name : Error in remove_post function - ' . $ex);
        }  
  }
 }
