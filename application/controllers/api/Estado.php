<?php 
/*
    Generated by Manuigniter v2.0 
    www.manuigniter.com
*/
use Restserver\Libraries\REST_Controller;
defined('BASEPATH') OR exit('No direct script access allowed');
require APPPATH . '/libraries/REST_Controller.php';
require APPPATH . 'libraries/Format.php';
class Estado extends REST_Controller{
 function __construct()
 {
       parent::__construct();
      $this->load->model('Estado_model');
 } 
 /*
* Listing of estado
 */
public function get_all_post()
{
  try{
  $data['estado'] = $this->Estado_model->get_all_estado();
     if($data['estado'] && $data['estado']!=null){
       $estado_ar = array();
       foreach($data['estado'] as $e)
       {
       $e_ar = array();
       $e_ar['estado_id'] = $e['estado_id'];
       $e_ar['estado_descripcion'] = $e['estado_descripcion'];
       $e_ar['estado_tipo'] = $e['estado_tipo'];
       $e_ar['estado_color'] = $e['estado_color'];
       $estado_ar[] = $e_ar;
       }
       $response = array(
       'status' => 200,
       'message' => 'get all data Succesfully',
       'data' => $estado_ar,
       );
       $this->response($response, REST_Controller::HTTP_OK);
     }
     else{
       $response = array(
      'status' => 400,
      'message' => 'Detail is not found'
        );
       $this->response($response, REST_Controller::HTTP_OK); 
        }
       } catch (Exception $ex) {
             throw new Exception('Estado controller_name : Error in get_all_post function - ' . $ex);
        }  
}
 /*
  * Adding a new estado
  */
 function addnew_post()
 {  
  try{
      $params = array(
       'estado_descripcion'=> $this->input->post('estado_descripcion'),
       'estado_tipo'=> $this->input->post('estado_tipo'),
       'estado_color'=> $this->input->post('estado_color'),
        );
       $this->load->library('upload');
       $this->load->library('form_validation');
       if(isset($_POST) && count($_POST) > 0)     
        {  
            $estado_id= $this->Estado_model->add_estado($params);
   $data['estado'] = $this->Estado_model->get_estado($estado_id);
           $response = array(
            'status' => 200,
            'message' => 'Succesfully Added',
            'data' => $data
             );
           $this->response($response, REST_Controller::HTTP_OK);
        }
        else
        { 
           $response = array(
            'status' => 400,
            'message' => 'Not Added try again',
            'data' => ''
             );
           $this->response($response, REST_Controller::HTTP_OK);
        }
       } catch (Exception $ex) {
             throw new Exception('Estado controller_name : Error in new estado function - ' . $ex);
       }  
 }  
  /*
  * Editing a estado
 */
  function edit_post($estado_id)
 {   
  try{
   $data['estado'] = $this->Estado_model->get_estado($estado_id);
       $this->load->library('upload');
       $this->load->library('form_validation');
     if(isset($data['estado']['estado_id']))
      {
        $params = array(
           'estado_descripcion'=> $this->input->post('estado_descripcion'),
           'estado_tipo'=> $this->input->post('estado_tipo'),
           'estado_color'=> $this->input->post('estado_color'),
        );
          if(isset($_POST) && count($_POST) > 0)     
           {  
           $this->Estado_model->update_estado($estado_id,$params);
           $response = array(
            'status' => 200,
            'message' => 'Succesfully Updated',
            'data' => $estado_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
           }
           else
          {
           $response = array(
            'status' => 400,
            'message' => 'Not Updated Try again',
            'data' => $estado_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
          }
  }
       } catch (Exception $ex) {
             throw new Exception('Estado controller_name : Error in edit_post function - ' . $ex);
        }  
} 
/*
  * Deleting estado
  */
  function remove_post($estado_id)
   {
  try{
      $estado = $this->Estado_model->get_estado($estado_id);
  // check if the estado exists before trying to delete it
       if(isset($estado['estado_id']))
       {
         $this->Estado_model->delete_estado($estado_id);
           $response = array(
            'status' => 200,
            'message' => 'Succesfully Removed',
            'data' => $estado_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
       }
       else
           $response = array(
            'status' => 400,
            'message' => 'Not Updated Try again',
            'data' => $estado_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
       } catch (Exception $ex) {
             throw new Exception('Estado controller_name : Error in remove_post function - ' . $ex);
        }  
  }
 }
