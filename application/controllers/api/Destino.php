<?php 
/*
    Generated by Manuigniter v2.0 
    www.manuigniter.com
*/
use Restserver\Libraries\REST_Controller;
defined('BASEPATH') OR exit('No direct script access allowed');
require APPPATH . '/libraries/REST_Controller.php';
require APPPATH . 'libraries/Format.php';
class Destino extends REST_Controller{
 function __construct()
 {
       parent::__construct();
      $this->load->model('Destino_model');
 } 
 /*
* Listing of destino
 */
public function get_all_post()
{
  try{
  $data['destino'] = $this->Destino_model->get_all_destino();
     if($data['destino'] && $data['destino']!=null){
       $destino_ar = array();
       foreach($data['destino'] as $d)
       {
       $d_ar = array();
       $d_ar['destino_id'] = $d['destino_id'];
       $d_ar['destino_nombre'] = $d['destino_nombre'];
       $d_ar['destino_ubicacion'] = $d['destino_ubicacion'];
       $destino_ar[] = $d_ar;
       }
       $response = array(
       'status' => 200,
       'message' => 'get all data Succesfully',
       'data' => $destino_ar,
       );
       $this->response($response, REST_Controller::HTTP_OK);
     }
     else{
       $response = array(
      'status' => 400,
      'message' => 'Detail is not found'
        );
       $this->response($response, REST_Controller::HTTP_OK); 
        }
       } catch (Exception $ex) {
             throw new Exception('Destino controller_name : Error in get_all_post function - ' . $ex);
        }  
}
 /*
  * Adding a new destino
  */
 function addnew_post()
 {  
  try{
      $params = array(
       'destino_nombre'=> $this->input->post('destino_nombre'),
       'destino_ubicacion'=> $this->input->post('destino_ubicacion'),
        );
       $this->load->library('upload');
       $this->load->library('form_validation');
       if(isset($_POST) && count($_POST) > 0)     
        {  
            $destino_id= $this->Destino_model->add_destino($params);
   $data['destino'] = $this->Destino_model->get_destino($destino_id);
           $response = array(
            'status' => 200,
            'message' => 'Succesfully Added',
            'data' => $data
             );
           $this->response($response, REST_Controller::HTTP_OK);
        }
        else
        { 
           $response = array(
            'status' => 400,
            'message' => 'Not Added try again',
            'data' => ''
             );
           $this->response($response, REST_Controller::HTTP_OK);
        }
       } catch (Exception $ex) {
             throw new Exception('Destino controller_name : Error in new destino function - ' . $ex);
       }  
 }  
  /*
  * Editing a destino
 */
  function edit_post($destino_id)
 {   
  try{
   $data['destino'] = $this->Destino_model->get_destino($destino_id);
       $this->load->library('upload');
       $this->load->library('form_validation');
     if(isset($data['destino']['destino_id']))
      {
        $params = array(
           'destino_nombre'=> $this->input->post('destino_nombre'),
           'destino_ubicacion'=> $this->input->post('destino_ubicacion'),
        );
          if(isset($_POST) && count($_POST) > 0)     
           {  
           $this->Destino_model->update_destino($destino_id,$params);
           $response = array(
            'status' => 200,
            'message' => 'Succesfully Updated',
            'data' => $destino_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
           }
           else
          {
           $response = array(
            'status' => 400,
            'message' => 'Not Updated Try again',
            'data' => $destino_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
          }
  }
       } catch (Exception $ex) {
             throw new Exception('Destino controller_name : Error in edit_post function - ' . $ex);
        }  
} 
/*
  * Deleting destino
  */
  function remove_post($destino_id)
   {
  try{
      $destino = $this->Destino_model->get_destino($destino_id);
  // check if the destino exists before trying to delete it
       if(isset($destino['destino_id']))
       {
         $this->Destino_model->delete_destino($destino_id);
           $response = array(
            'status' => 200,
            'message' => 'Succesfully Removed',
            'data' => $destino_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
       }
       else
           $response = array(
            'status' => 400,
            'message' => 'Not Updated Try again',
            'data' => $destino_id
             );
           $this->response($response, REST_Controller::HTTP_OK);
       } catch (Exception $ex) {
             throw new Exception('Destino controller_name : Error in remove_post function - ' . $ex);
        }  
  }
 }
